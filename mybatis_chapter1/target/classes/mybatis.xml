<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 引入jdbc配置文件 -->
	<properties resource="jdbc.properties">
	</properties>
	<!-- 全局配置，延迟加载 -->
	<settings>
		<setting name="lazyLoadingEnabled" value="true"/>
		<setting name="aggressiveLazyLoading" value="false"/>
	</settings>
	<!-- 取别名 -->
	<typeAliases>
		<typeAlias type="com.woniuxy.mybatis.entity.User" alias="user" />
		<typeAlias type="com.woniuxy.mybatis.entity.Student" alias="student" />
		<typeAlias type="com.woniuxy.mybatis.entity.IdCard" alias="idCard" />
		<typeAlias type="com.woniuxy.mybatis.entity.Health" alias="health" />

	</typeAliases>
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${jdbc.driver}" />
				<property name="url" value="${jdbc.url}" />
				<property name="username" value="${jdbc.username}" />
				<property name="password" value="${jdbc.password}" />
			</dataSource>
		</environment>
	</environments>
	<mappers>
		<mapper resource="mappings/User.xml" />
		<mapper resource="mappings/Uuid.xml" />
		<mapper resource="mappings/Mapper.xml" />
		<mapper resource="mappings/studentMapper.xml" />

	</mappers>
</configuration>